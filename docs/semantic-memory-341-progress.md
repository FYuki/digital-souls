# #341 意味記憶の管理と実モデル調整記録

2026-09-15時点。これは途中の実装・調整証跡であり、Epic完了またはモデル採用の証明ではない。

## 実装

- 自己申告から直接取得した意味記憶だけを、値の入力でUI訂正できる。形成経路・所有者・自己申告フラグはAPIで変更させない。
- UI訂正はMANUAL根拠を持つ新しい意味記憶として記録する。元会話を改変せず、旧知識とのCORRECT関係、出典版、適用時期を監査画面で追える。
- 版の競合は409、許可されない訂正・privacy拒否は本文を含まない422となる。同じ要求の再送は同じ結果を返し、キーを違う訂正内容に再利用できない。
- 個別レコードの削除はその全版の本文を消し、同じ出典・版による復活を抑止する。元の会話とEpisodeを保持する。
- CORRECT関係でつながる別レコードまで一括削除する拡張は、削除範囲の回答待ちで未実装。
- LLMが一般化に対してCORRECTを提案しても、直接の自己申告を理由に一般化を停止させない契約を保存側でも保証する。

## モデル判断の修正

固定ケースのSHA256は `1603788df455b6598ab328468c9fe048692efce51c851b87ab0749efc6fdd676` のまま変更していない。

semantic-v3の診断では、会話文脈を保存済み知識と取り違え、存在しないtargetを返す問題が判明した。
semantic-v4はNEWと更新操作の組合せをJSON schemaで制約し、更新先を同じキャラクターの提示済みIDに限定する。
明示訂正、固定属性の矛盾、時間変化、自己申告優先の判定順を共通promptへ明記した。

privacy方針は2026-09-semantic-v1、分類promptはsemantic-privacy-prompt-v12。
「保存禁止」「記憶に残さないで」等の保存拒否表現を追加した。
合意要件で保存対象となる誕生日の月日、概括的な居住地域、好みを、それだけでOTHER_SENSITIVEとしないよう分類指示を明確化した。
正確な住所・連絡先・健康・金融・非公開の第三者情報などの拒否条件と、原発言全体・全slotの検査は保持する。
既存privacy固定fixtureはpolicy_versionのみ更新し、期待する分類や根拠範囲は変更していない。
共通privacyの実モデルconformanceは専用Ollamaのe4bで実施した。ADMISSION/QUERY_GATE各60ケース、
判定保留・偽陰性・偽陽性はともに0件。証跡は`/tmp/privacy-eval-i4psfe0z`。

## 確認済み

| 検証 | 結果 |
|---|---|
| privacy unit/moduleと意味記憶store/runtime/management | 378 passed |
| backendの設定を使ったmypy | 285 files、エラーなし |
| backend appと変更テストのruff | 成功 |
| frontendのSvelte・TypeScript検査 | エラー0、警告0 |
| 意味記憶管理UIのunit | 4 passed |
| PR #400のCI | backend/frontend/container-contracts/mocked-e2e成功、Epicへ統合済み |

semantic-v4、12b、100ケース×1回の調整用試行では、全10分類が90%以上、禁止情報保存・キャラクター混入0件。
直接知識100%、出来事除外90%、明示確認90%、再言及100%、時間変化90%、明示訂正90%、固定属性矛盾90%、自己申告優先100%、根拠除外90%、安全境界100%。
一部ケースの判断誤りや実行エラーは残る。各モデル3回の正式評価を代替しない。

同じsemantic-v4のe4b試行は、直接知識80%、出来事除外10%、明示確認20%、再言及80%、時間変化100%、
明示訂正100%、固定属性矛盾40%、自己申告優先90%、根拠除外20%、安全境界50%。
禁止した合成ケース5件で保存が発生したため不合格。証跡は`/tmp/ds341-eval-v4-e4b-01`。
現時点では12bが有力だが、正式採用はしていない。

元の機械可読証跡は専用ローカルディレクトリ `/tmp/ds341-eval-v4-12b-01` に保存した。
manifestのcommit・ファイルhash・モデルdigest・量子化・設定と、run-1.jsonlの実保存結果を参照する。
Ollamaの読み込みメモリsnapshotはピーク使用量や会話への影響の実測とは区別する。

## 残る作業

先行のsemantic-v4では正式比較600ケース・資源観測・ブラウザ操作・実会話を検証し、品質基準は12bのみ合格。
修正後のsemantic-v6も全600ケースが完了し、12bの3回はすべて合格、E4Bの3回はすべて不合格。
最新実装で矛盾後のUI訂正、実索引・別会話への反映、UI削除後の本文消去・参照除外を実接続で確認した。
会話優先の中断・再試行を実装し、実Ollamaで25〜66msの中断と未処理予約の回復を確認した。
並走時の応答は0.907〜9.843秒で、モデルの再読込待ちと一部の想起には制限が残る。
公開push・CI・Epic統合は未完了であり、Epic完了とはしていない。
最新の証跡と失敗を含む結果は[正式評価・実接続記録](validation/semantic-memory-341-2026-09-15.md)を参照する。

入力予算超過時の候補選択を追加し、全件が収まる場合の保持、多数の無関係な知識から関連属性を選ぶこと、
並べ替え後も正しいIDだけを更新することを4テストで検証した。共通の文字的一致による選択であり、同義語の網羅は保証しない。
実接続用の`acceptance_semantic_memory.py`とメモリ量の観測器も準備した。準備・readinessは実会話受入の証明に数えない。
mainへのマージはユーザーが行う。


## 再言及後の応答参照の回帰

本人の明示確認で再言及された知識について、文脈に使ったassistant発言の古い記憶版を継承すると、
新版を引用した後続応答まで失効する不具合を再現した。
直接取得は本人の申告・確認を独立した根拠とし、文脈の発言自体の版検証は保持しつつ、
そのassistant発言が参照した古い記憶版への依存は継承しない。
assistantのみを根拠とする直接取得は有効にしない。経験由来の知識とEpisode/Factの依存検証は保持する。

再言及の回帰、訂正・削除・循環・キャラクター分離を含む41テストが成功。
別途、管理操作→実SQLite/Chroma→版付き応答参照→訂正・削除後の検索除外の接続試験が成功した。
この接続試験のEmbeddingとprivacyは合成実装であり、実LLM・実Embeddingの受入とは区別する。

## 会話開始時の中断・再試行

`c06d3d3`でEpisode・意味記憶workerの実行中HTTPを中断可能にした。
元の会話と完了済み記憶を保持し、未完了出典を次の待機時に再処理する。
実モデルの4回の中断試験後、11会話の原文保持、予約・index待機0件、同一出典と内容の重複0件を確認した。
好み8件の再言及は同じ8レコードの第3版となり、追加の1件と合わせて意味記憶9レコードだった。
関連154テスト、mypy 290ファイル、ruffが成功。詳しい制限と失敗を含む実測は
[中断・再試行の検証記録](validation/semantic-memory-341-2026-09-15.md)を参照する。
